version: '3'

services:
  database:
    image: postgres
    networks:
      - dev
    environment:
      - POSTGRES_USER:postgres
      - POSTGRES_DB=simple-http-server
#    ports:           tomuto neverim, ze to treba
#      - 5432:5432

  server:
#    build: "."
    image: s-reflexom
    depends_on:
      - database
    networks:
      - dev
    ports:
      - 50000:50000
    volumes:
     - "/home/luppolo/go/src/github.com/ololko/simple-HTTP-server:/app"

networks:
  dev: